# Copy this file to .env and fill in real values.
# Load it with `python-dotenv` (or your process manager) before starting main.py.

# Alpha Vantage
ALPHA_VANTAGE_API_KEY=LZ3H22RO48J0NH0I

# Postgres
DB_HOST=localhost
DB_NAME=investment
DB_USER=postgres
DB_PASSWORD=Beninkk90$

# Optional: override where predictgrowth.py looks for the trained model
STOCK_MODEL_PATH=stock_prediction_model.pkl

VITE_API_BASE_URL=http://localhost:8000